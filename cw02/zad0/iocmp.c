#include <stdlib.h>
#include "iocmp.h"

int generate (char *name) {
    FILE *file = fopen(name, w);

    return 0;
}

int sort (char *name) {

    return 0;
}

int copy (char *name1, char *name2, int record_size) {

    return 0;
}
